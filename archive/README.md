# archive — 구 정본 시대의 도구·문서

이 저장소가 정본으로 승격(2026-08-29)되기 전, 상위 플랫폼 저장소(`ggc_ai_platform`)가
정본이던 시기의 파일이다. **더 이상 실행·참조 대상이 아니다** — 이력 이해를 위해 보존한다.
`tools/check-links.py` 의 금지 문자열 검사도 이 폴더는 건너뛴다.

| 파일 | 무엇이었나 |
|---|---|
| `sync-from-platform.sh` | 상위 저장소 → 이 저장소 복사·재마스킹 스크립트. 정본이 여기로 오면서 방향 자체가 사라졌다 |
| `redaction.local.sed.example` | 위 스크립트가 쓰던 마스킹 치환 규칙 예시 |
| `03-공통디자인가이드.md` | 공통 가이드 v1.0 (2026-07-12) — 값이 v1.1 이전 기준이라 낡았다. 현행 규칙은 `../docs/contract.md`, 현행 값은 `../design/ggc-tokens.css` |
